# Hyacinth — Ita-Bag Display App

## Context
The user owns a tote bag with a transparent side window and wants to permanently mount an Android tablet inside it as an always-on "Ita-Bag" display. Hyacinth is a greenfield project: a Flutter Android app that acts as a home launcher and renders remote-driven content, paired with a tiny single-file Go server that publishes config and resource packs. The working directory `/home/nvirellia/Projects/hyacinth` is empty.

Goals:
- Tablet boots straight into Hyacinth (registered as launcher) and shows fullscreen, immersive content.
- Content + display settings (brightness, screen timeout) are pushed live from a self-hosted server.
- Content can be a remote URL or a locally-cached "resource pack" (zipped Vite build or single image), fetched only over Wi-Fi.
- Robust fallback UI (HealthCheck + Settings) when things aren't configured or working.

## Repo Layout
```
hyacinth/
├── client/                       # Flutter app
│   ├── android/app/src/main/AndroidManifest.xml
│   ├── android/app/src/main/res/xml/network_security_config.xml
│   ├── pubspec.yaml
│   └── lib/
│       ├── main.dart                    # Entry, routing on AppState
│       ├── app_state.dart               # State machine ChangeNotifier
│       ├── config/
│       │   ├── config_model.dart        # HyacinthConfig (with == for diffing)
│       │   └── config_store.dart        # SharedPreferences: server URL, last cfg
│       ├── net/
│       │   ├── config_client.dart       # GET /config
│       │   └── ws_client.dart           # /ws + reconnect/backoff/heartbeat
│       ├── display/
│       │   ├── display_page.dart        # Immersive WebView host
│       │   └── webview_controller.dart  # flutter_inappwebview wrapper, reload-guard
│       ├── fallback/
│       │   ├── main_activity_page.dart  # HealthCheck + Settings
│       │   ├── health_check.dart
│       │   └── settings_page.dart
│       ├── permissions/perm_manager.dart
│       ├── system/
│       │   ├── brightness.dart          # screen_brightness package
│       │   ├── keep_awake.dart          # wakelock_plus
│       │   ├── immersive.dart           # SystemChrome immersive sticky
│       │   ├── secure_settings.dart     # MethodChannel: Settings.Global/System via WRITE_SECURE_SETTINGS
│       │   ├── keyguard.dart            # DevicePolicyManager + KeyguardLock
│       │   └── cpu_governor.dart        # Root: write scaling_governor / scaling_max_freq
│       ├── resource_pack/
│       │   ├── pack_manager.dart        # ensure(), download, unzip, swap
│       │   ├── pack_cache.dart          # disk layout + GC
│       │   ├── wifi_guard.dart          # connectivity_plus
│       │   └── scheme_handler.dart      # hyacinth:// resolver
│       └── onboarding/onboarding_page.dart
└── server/
    ├── go.mod
    ├── server.go                 # Everything: handlers + inlined HTML/CSS/JS
    └── data/                     # config.json + packs/
```

## Android Manifest Essentials
- Permissions: `INTERNET`, `ACCESS_NETWORK_STATE`, `ACCESS_WIFI_STATE`, `WAKE_LOCK`, `POST_NOTIFICATIONS` (API 33+), `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`, `FOREGROUND_SERVICE`, **`WRITE_SECURE_SETTINGS`** (granted via `adb`/root `pm grant io.hyacinth android.permission.WRITE_SECURE_SETTINGS`), **`DISABLE_KEYGUARD`**.
- Main `<activity>` gets two intent filters: standard `MAIN/LAUNCHER` plus `CATEGORY_HOME + CATEGORY_DEFAULT` (home launcher). `showWhenLocked="true"` + `turnScreenOn="true"` so content reappears when waking.
- `android:launchMode="singleTask"`, `excludeFromRecents="true"`, `resizeableActivity="false"`.
- `network_security_config.xml` permitting cleartext only for private IP ranges.
- Custom `hyacinth://` is intercepted **inside** the WebView — does NOT need a manifest filter.
- **Device Admin receiver**: `<receiver android:name=".admin.HyacinthAdminReceiver" android:permission="android.permission.BIND_DEVICE_ADMIN">` with `DEVICE_ADMIN_ENABLED` intent filter and a `device_admin.xml` declaring `disable-keyguard-features` (and `force-lock` if we ever want `lockNow()`). User activates it via `DevicePolicyManager.ACTION_ADD_DEVICE_ADMIN` in onboarding.

## State Machine
States: `Booting → Onboarding? → Connecting → Displaying ⇄ Reconnecting → Fallback`.

1. `main.dart` reads `ConfigStore`. If no server URL or onboarding incomplete → **Onboarding** (explain → request notifications → battery-opt exemption → home-role via `RoleManager` / `Settings.ACTION_HOME_SETTINGS` → enter server URL → save).
2. **Connecting**: run `HealthCheck`; basic fail → Fallback. Otherwise GET `/config`.
3. On success: apply brightness + screenTimeout, enable immersive, mount `DisplayPage` with initial `contentUrl`. Open WS in parallel.
4. **Displaying**: `WsClient` events call `applyConfig(new)`:
   - Brightness changed → update brightness only.
   - Timeout changed → update wakelock only.
   - `contentUrl + contentRevision` unchanged → **do nothing to WebView** (avoids flicker).
   - Otherwise → `webview.loadUrl(new.contentUrl)`.
   The ONLY path that calls `loadUrl` is `_maybeReloadContent(old, new)` guarded by URL+revision equality.
5. Errors (HTTP fail, WS drop past N retries, perm revoked) → Fallback, which keeps retrying in the background and auto-recovers to Displaying.
6. Re-apply immersive on `didChangeAppLifecycleState` (dialogs can break sticky immersive).

Fallback `MainActivity` page:
- HealthCheck rows with green/red + "Fix" buttons that fire the right intent.
- Settings: server URL field with "Test connection", "Re-request permissions", "Clear pack cache", "Reload now".

## Resource Packs
**Format**: single image (`png/jpg/webp/gif`) OR `.zip` containing a Vite `dist/` with `index.html` at archive root. Each pack has a server-side manifest: `{id, version, type, filename, sha256, size, createdAt}`.

**Device storage** (under `getApplicationSupportDirectory()`):
```
packs/<pack_id>/
  current  -> pointer file naming active version
  <version>/
    manifest.json
    content/         # unzipped files OR image.ext
```

**Resolution in WebView**: config `content` is either `https://…` or `hyacinth://pack/<id>/<path>`. Use `flutter_inappwebview`'s custom scheme handler (`resourceCustomSchemes` + `CustomSchemeResponse`) to map requests to files under `packs/<id>/current/content/`. Fallback option if scheme handler is fragile: tiny in-process loopback `HttpServer` on `127.0.0.1`. **Decide custom-scheme first; loopback only if it breaks.**

**Fetch strategy**:
- `WifiGuard` (connectivity_plus) gates downloads to `ConnectivityResult.wifi`. On mobile, defer + surface as a non-fatal health warning.
- `PackManager.ensure(packId)`: GET manifest → compare to local `current` → if version differs, GET download → stream to temp → verify sha256 → unzip into `<version>/content/` → atomically swap `current` pointer → keep last 1 version for rollback.
- Pre-warm before `loadUrl`. Manual "Clear pack cache" in fallback.

## Server (`server.go`)
Stdlib `net/http` + `github.com/gorilla/websocket`. Single file. `data/config.json` and `data/packs/index.json` written via tmp + rename for atomicity. Mutex around mutations. WS broadcast via `map[*Conn]bool` + write mutex.

**Endpoints**
- `GET  /config` → JSON:
  ```json
  {
    "content": "hyacinth://pack/neko/index.html",
    "contentRevision": "2026-04-07T10:15:00Z",
    "brightness": "auto",
    "screenTimeout": "always-on"
  }
  ```
  (`brightness`: `"auto"` or 0–100; `screenTimeout`: `"always-on"` or duration like `"30s"`/`"5m"`.)

  Additional optional fields:
  - `disableKeyguard`: bool — if true, dismiss keyguard via DevicePolicyManager so the tablet wakes directly into the WebView (no swipe-to-unlock).
  - `powerProfile`: `"normal" | "ultra-low"` — when `"ultra-low"` and root is available, set CPU governor to `powersave` and clamp `scaling_max_freq` to the lowest available frequency while content is displayed; restore on Fallback / config change.
  - `touchInput`: `"enabled" | "disabled"` (default `"disabled"`) — when disabled, the Displaying state swallows all touch events so accidental bag bumps can't interact with the WebView. A long-press hot-corner (e.g. 5s in top-left) escapes to Fallback for maintenance.
- `PUT  /config` (operator) → replace; bump `contentRevision` if `content` changed; broadcast to WS.
- `GET  /ws` → WebSocket. Envelopes: `{"type":"config_update","config":{...}}`, `{"type":"ping"}`/`pong`. Client ignores unknown types (forward compat).
- `GET  /packs`, `GET /packs/{id}/manifest`, `GET /packs/{id}/download`.
- `POST /packs` (multipart: `id`, `type`, `file`) → validate (zip must contain `index.html`, or allowed image), sha256, write to `data/packs/<id>/<version>/`, update index.
- `DELETE /packs/{id}`.
- `GET  /` → inlined operator HTML (single Go `const indexHTML` with `<style>` + `<script>` blocks; mobile-first viewport, single column; sections for current config form (content URL select populated from `/packs` + free-form, brightness slider + auto toggle, timeout select) and pack list with upload + delete). Plain `fetch()`, no framework, no build step.
- All operator endpoints (`PUT /config`, `POST /packs`, `DELETE`) gated by a shared-secret token (header), even on LAN.

## Key Decisions Already Made
- **WebView**: `flutter_inappwebview` (custom scheme handlers + request interception). `webview_flutter` is insufficient.
- **Brightness**: prefer `screen_brightness` package (window brightness — no permission). For *system* brightness (so the value persists across wake), use `WRITE_SECURE_SETTINGS` granted via root `pm grant` to write `Settings.System.SCREEN_BRIGHTNESS` / `SCREEN_BRIGHTNESS_MODE`.
- **Screen timeout**: use `WRITE_SECURE_SETTINGS` to set `Settings.System.SCREEN_OFF_TIMEOUT` directly (cleaner than wakelock hacks). `"always-on"` → `Integer.MAX_VALUE`. Wakelock remains as a belt-and-suspenders.
- **Keyguard**: Device Admin receiver + `DevicePolicyManager.setKeyguardDisabledFeatures(KEYGUARD_DISABLE_FEATURES_ALL)`, plus `KeyguardManager.requestDismissKeyguard()` from the activity. Activated through onboarding (system dialog).
- **Touch blocking**: in `DisplayPage`, wrap the WebView in an `AbsorbPointer(absorbing: true)` (or `IgnorePointer`) above it, plus a transparent `Listener` overlay that *only* watches for the maintenance gesture (5-second press in a designated corner) to flip back to Fallback. The WebView itself never receives touches in this mode. Also call `getWindow().addFlags(FLAG_NOT_TOUCHABLE)` via MethodChannel as a second layer if needed (note: this would block our overlay too, so prefer the Flutter-side approach). For root devices, optionally `chmod 000` / `setenforce`-style tricks are NOT used — Flutter-side blocking is sufficient.
- **Root + CPU governor** (`powerProfile: "ultra-low"`): via a `Runtime.exec("su")` MethodChannel helper, write `powersave` to `/sys/devices/system/cpu/cpufreq/policy*/scaling_governor` and the lowest value from `scaling_available_frequencies` to `scaling_max_freq`. Restore previous values on teardown / when leaving Displaying. Treat root as optional — gracefully no-op (with health warning) if `su` fails.
- **Keep awake**: `wakelock_plus` + `FLAG_KEEP_SCREEN_ON`. Battery-opt exemption is required.
- **WS liveness**: app-side ping ~20s, exponential backoff with jitter. Add a foreground service later (M8) to hold the WS during Doze.
- **Cleartext**: scoped via `network_security_config.xml`, not global.
- **WebView security**: disable `file://`, allow only `https:` and the custom scheme.
- **Pack atomicity**: never unzip into `current/`; always to a version dir, then swap pointer.

## UI Style
All Flutter UI uses **Material You (Material Design 3)**: `useMaterial3: true` on `ThemeData`, `ColorScheme.fromSeed` (with a Hyacinth-purple seed), dynamic color where the device supports it (`dynamic_color` package), and Material 3 components throughout (`FilledButton`, `NavigationBar`, `Card`, `ListTile`, etc. — no Material 2 / Cupertino mixing). The Display page itself is bare WebView and exempt; all chrome (Onboarding, Fallback, Settings, future Operator-side mobile views) follows M3.

## Per-Milestone Testing Requirement
Every milestone MUST include real automated tests for the code it adds, and `flutter test` (client) + `go test ./...` (server) must be green before the milestone is marked complete. Tests should exercise behavior, not just construct widgets. Specifically:
- New units (state machines, parsers, clients, packers) get unit tests with hand-rolled fakes — no mockito.
- New widgets get at least one widget test that asserts a real invariant (rendered text, button presence, state-after-tap), not "it doesn't crash."
- Server endpoints get `httptest`-based tests covering happy path + one failure mode.
- A milestone with the excuse "tests need a real device" must still ship hermetic unit tests for whatever can be tested off-device.

## Build Order (Milestones)
- **M0 — Skeletons**: `flutter create client`, `go mod init`, empty `server.go` returning hardcoded `/config` JSON. Client GETs and prints. Prove end-to-end connectivity.
- **M1 — Minimum viable display**: `flutter_inappwebview` rendering an `https://` URL fullscreen + immersive + wakelock. Persist server URL.
- **M2 — Fallback + health + onboarding**: `AppState` machine, MainActivity fallback, HealthCheck, onboarding wizard, permission prompts, `CATEGORY_HOME` filter + home-role flow.
- **M2.5 — Material You audit + test backfill**: Convert all existing client UI (Onboarding, Fallback/MainActivity, Settings, loading/error screens) to Material You / Material Design 3 (`useMaterial3: true`, `ColorScheme.fromSeed`, `dynamic_color` where supported, M3 components). Backfill the per-milestone testing requirement for M0/M1/M2 wherever it's missing: hermetic unit tests for `HyacinthConfig`, `ConfigStore`, `ConfigClient` (with `package:http/testing` MockClient), `AppState` (transitions, fallback retry timer cancel-on-dispose, recheckPermissions flips out of `displaying` when a check goes red), and `HealthCheck`; widget tests for `OnboardingPage` covering all five steps via injected fakes; server-side `httptest` for `/config` and `/health`. Also wire the lifecycle-resume → `recheckPermissions()` hook that M2 deferred.
- **M3 — WebSocket live updates**: `/ws` broadcast on `PUT /config`. Client `WsClient` with reconnect. Implement and verify "don't reload if unchanged" guard (toggle brightness/timeout without WebView flicker).
- **M4 — Operator frontend inlined** in `server.go`. Use **Material Design 3** styling for the inlined HTML wherever possible: import the official `@material/web` CDN bundle and use `<md-filled-button>`, `<md-outlined-text-field>`, `<md-list>`, `<md-fab>` etc.; pair with Material 3 color tokens (CSS custom properties) seeded from a Hyacinth-purple primary, dark-mode via `prefers-color-scheme`. The constraint is "single inlined HTML, no build step, plain `fetch()`" — `@material/web` is a single ESM import URL and meets that bar. If a Material 3 component is genuinely incompatible with the no-build constraint for one specific control, fall back to a hand-styled element that visually matches the M3 spec rather than dropping back to plain HTML defaults.
- **M4.5 — Utilities (Justfile + full test/build)**: Add a top-level `Justfile` with at minimum: `testall` (runs `cd client && flutter test` AND `cd server && go test ./...`), `analyze` (runs `cd client && flutter analyze` AND `cd server && go vet ./...`), `apk` (runs `cd client && flutter build apk --release`), `apk-debug`, `server-run` (runs `cd server && go run .`), `fmt`, and a default `just` listing the recipes. Document the Justfile briefly in a top-level `README.md` (or extend an existing one). After committing the Justfile, run `just testall` AND `just apk` end-to-end and capture the outputs in the milestone report. Fix anything that breaks during the real build (this is the first time the APK is actually built; expect manifest/permission/SDK-version issues to surface and require fixes).
- **M4.6 — `apk-install` recipe**: add a Just recipe that uses `adb install -r` to push the release APK to a connected device, depending on `apk` so it always installs the freshest build.
- **M4.7 — Home action shows MainActivity**: When Hyacinth is the active launcher and the user performs the Home gesture, the activity is relaunched and the fullscreen WebView flashes/tears down. Treat Home as an explicit "show me the main activity" intent: (a) set `android:launchMode="singleTask"` on `MainActivity` so the activity instance is reused instead of recreated; (b) override `onNewIntent` in `MainActivity.kt` and, when the new intent carries `CATEGORY_HOME`, post a `home_pressed` event over a MethodChannel `io.hyacinth/home_intent` to Flutter; (c) Flutter listens at app boot and calls a new `AppState.requestMainActivity()` which transitions the phase to `fallback` while *preserving* `_config` (so the cached `HyacinthConfig` is still available); (d) add a prominent `FilledButton` "Return to content" on `MainActivityPage`, visible only when `appState.config != null`, which calls a new `AppState.returnToDisplaying()` that transitions back to `displaying` using the cached config (no re-fetch). Add `AppState` unit tests for both transitions and a widget test asserting the button appears only when a config is cached.
- **M4.8 — Drop launcher functionality**: M4.7 didn't fix the underlying flicker because the launcher integration is fundamentally fighting Android's task semantics. The user has explicitly said they will not "exit" the app, so Hyacinth no longer needs to be a launcher at all. Strip launcher integration completely: (a) remove the `MAIN`/`CATEGORY_HOME`/`CATEGORY_DEFAULT` intent filter from `AndroidManifest.xml` (keep the standard `MAIN`/`LAUNCHER` filter so the app is launchable from the app drawer); (b) revert `MainActivity.kt` to a plain `FlutterActivity` (delete the `io.hyacinth/home_intent` MethodChannel registration and `onNewIntent` override); (c) delete the Flutter-side MethodChannel listener in `_HyacinthAppState`; (d) delete `AppState.requestMainActivity()` and `AppState.returnToDisplaying()`; (e) delete the "Return to content" button on `MainActivityPage`; (f) remove the "home launcher role" check from `HealthCheck` and the "home role" step from `OnboardingPage` (so onboarding becomes 4 steps: explain → notifications → battery-opt → server URL); (g) remove the now-unused `android_intent_plus` dependency if nothing else uses it; (h) update all M4.7-era tests accordingly. Keep `android:launchMode="singleTask"` (it's a sensible default and not launcher-specific). After this milestone, Hyacinth is just a normal Android app whose `MainActivity` happens to render fullscreen content most of the time; users navigate to it from the app drawer.
- **M4.9 — Tablet-friendly landscape layout**: The tablet runs permanently in landscape, so the existing single-column UI on `OnboardingPage` and `MainActivityPage` looks horizontally stretched (long text rows, awkwardly wide cards, single huge button). Apply Material 3 responsive layout patterns: (a) introduce a tiny shared helper that classifies the current window into M3 size classes (compact `<600dp`, medium `600–839dp`, expanded `≥840dp`) — Flutter has no built-in M3 breakpoint API, so a 30-line `lib/system/window_size.dart` is fine; (b) constrain `OnboardingPage`'s content to a centered column with `maxWidth: 560dp` and add comfortable horizontal padding so the wizard sits as a centered card on landscape rather than spanning the full width; (c) refactor `MainActivityPage` so on `expanded` windows the HealthCheck card and the Settings card sit side-by-side in a two-column `Row` (HealthCheck left, Settings right, both with `Expanded`), and on compact/medium windows it falls back to the existing single-column layout — wrap in `LayoutBuilder` and switch on the breakpoint; (d) the status footer remains full-width below the columns; (e) verify with a widget test that pumps a 1280×800 surface and asserts both cards are present horizontally (no vertical scroll required) and a separate test that pumps a 600×900 surface and asserts the single-column layout. The fullscreen `DisplayPage` is unchanged. No new dependencies.
- **M4.10 — Switch to `gorilla/websocket`**: Replace `nhooyr.io/websocket` with `github.com/gorilla/websocket` in `server/server.go`. Update `go.mod`/`go.sum`, the upgrader/handler in `handleWS`, the test in `server_test.go` (the one that dials the WS via `httptest.NewServer`), and any helper that touches the connection set. The WS protocol on the wire (envelope shapes, ping/pong, close codes) stays exactly the same — only the library swaps. After the swap, `go test ./...` must be green and the operator UI's WS connection (which uses the browser's native `WebSocket`) must continue to work end-to-end.
- **M5 — Resource packs (image)**: upload/list/download, `PackManager` for images, `hyacinth://` handler, Wi-Fi guard, cache layout.
- **M6 — Resource packs (zip)**: zip validation, `archive` package unzip, atomic version swap, mime mapping, sha256 verify.
- **M7 — Brightness + timeout polish**: full `auto`/numeric brightness and `always-on`/duration timeout via `WRITE_SECURE_SETTINGS`. Document the one-time `pm grant` step in the README/onboarding.
- **M7.5 — Keyguard + power profile**: Device Admin receiver + onboarding step to enable it; keyguard-disable wired up. Root-gated CPU governor controller with restore-on-exit; surface root status in HealthCheck.
- **M8 — Hardening**: foreground service for WS, network security config, operator auth token, error telemetry in fallback, pack GC, tablet-specific tweaks. **Server error hardening (explicit user requirement)**: every handler must return correct HTTP status codes for every error path, never panic, never leak goroutines, validate all inputs (JSON schema, content-types, sizes, paths for traversal), use `context.Context` for cancellation through every long-running operation, set explicit `http.Server` timeouts (`ReadHeaderTimeout`, `ReadTimeout`, `WriteTimeout`, `IdleTimeout`), structured error responses (`{"error": "..."}` with stable error codes), recover from panics in handler middleware, log every server-side error with sufficient context to debug it from logs alone, and add tests for the error paths (not just happy paths). The audit list: `GET/PUT /config` (oversized body, malformed JSON, missing fields, wrong content-type, concurrent PUT race), `GET /ws` (upgrade failure, slow client backpressure, write deadline expiration, max connection cap), `POST /packs` (multipart parse failure, file size cap, sha256 mismatch, zip-bomb defense, path traversal in `id`), `GET /packs/{id}/download` (not-found, range request handling, partial-write recovery), `DELETE /packs/{id}` (race against in-progress download). Every error path gets a test.
- **M8.1 — Root-based self-grant**: On a rooted tablet, the user shouldn't need to manually `adb` anything. Add a `RootHelper` that (a) probes root by running `su -c id` and parsing for `uid=0`, (b) exposes named grant methods that shell out to `su -c "<command>"` for: `pm grant io.hyacinth.hyacinth android.permission.WRITE_SECURE_SETTINGS`, `pm grant io.hyacinth.hyacinth android.permission.POST_NOTIFICATIONS`, and `dumpsys deviceidle whitelist +io.hyacinth.hyacinth`. The probe is gated to explicit user moments (onboarding entry, HealthCheck "Fix" buttons) — never run silently on every app start, since `su` triggers a Magisk/KernelSU consent dialog. Cache the result in SharedPreferences (`hyacinth.root.available` / `hyacinth.root.checked`) so subsequent launches can attempt silent grants without re-prompting. Onboarding gains a new optional "root check" step inserted after "Explain": it tries the probe, and if root is available, runs all three grants and reports the success/failure of each, then skips the notifications + battery steps for the grants that succeeded (re-checking via the existing perm_manager). HealthCheck gains a new "Root" info row (always green/grey, never red) showing the cached probe result, plus a "Try root grant" Fix button on any of the existing permission rows that are red. Tests cover: `RootHelper` MethodChannel wrapper (success / no su / non-zero exit / timeout), onboarding rooted path skips notif/battery, onboarding non-rooted path is unchanged. **Out of scope:** auto-running the probe on every app start, modifying selinux, anything that needs `su` outside the helper.
- **M8.2 — Back gesture exits to MainActivity**: M4.7 tried to bind the launcher Home gesture to "show MainActivity"; M4.8 stripped the whole launcher integration after it proved fragile. Now achieve the same UX via the back gesture, which is just an Activity-level event with no launcher plumbing required. (a) Wrap `DisplayPage` in a `PopScope(canPop: false, onPopInvokedWithResult: ...)` so the system back gesture is intercepted while in `displaying`; on intercept, call `AppState.requestMainActivity()`. (b) Re-introduce `AppState.requestMainActivity()` and `AppState.returnToDisplaying()` from M4.7 (without the MethodChannel plumbing): the former transitions phase to `fallback` while *preserving* `_config` and clearing `_error`; the latter transitions back to `displaying` reusing the cached config (rebuilds the WebView via the existing `_setPhase` lifecycle). (c) Add a prominent `FilledButton.icon` "Return to content" at the top of `MainActivityPage`, visible only when `appState.config != null`, calling `returnToDisplaying()`. (d) Back gesture from `MainActivityPage` itself uses default Android behavior (exits the app — the user can relaunch from the drawer). (e) Tests: AppState transitions (preserves config, clears error, no-op when already in target phase, WS lifecycle on both edges), `DisplayPage` PopScope intercepts back, `MainActivityPage` button visibility + tap behavior. The earlier M4.7 tests can serve as the template — they were deleted in M4.8. Bundled with M8.2 in the actual commit: rename the in-WebView custom scheme from `app-scheme://` to `hyacinth://` everywhere (server operator UI, client `scheme_handler`, `extractPackIdFromContent`, `webview_controller.resourceCustomSchemes`, all tests, plan.md), and fix a `OnboardingPage._runRootProbe` persistence bug where the production code path silently dropped the `setRootChecked`/`setRootAvailable` writes because the onboarding wizard was constructed without an injected `ConfigStore`; both fixes are covered in the same milestone since they were noticed during M8.2 review.
- **M8.3 — Pack cache sync + wipe**: Server-side `DELETE /packs/{id}` previously did not propagate to the client cache, so stale packs accumulated forever. (a) Add `PackCache.gcMissingPacks(Set<String> liveIds, {String? preserveId})` that walks the cache root and deletes any pack dir whose id isn't in `liveIds`, returning the deleted ids. `preserveId` is honored even when the id is absent from `liveIds` so a transient operator delete can't yank the currently-displayed pack. (b) Add `PackCache.wipeAll()` that nukes the entire cache root and recreates it empty. (c) Add `PackManager.syncToServer({String? preserveId})` that GETs `<base>/packs`, builds the live id set from the JSON array, and calls `cache.gcMissingPacks(...)`. Best-effort: any HTTP/JSON error is a no-op debug-printed and swallowed. (d) Wire `_connect()` in `AppState` to fire `syncToServer(preserveId: extractPackIdFromContent(cfg.content))` after `_ensurePackForConfig` as fire-and-forget so it never blocks the path to `displaying`. (e) Wire the previously-stub "Clear pack cache" button in `SettingsBlock` to call `appState.packCache.wipeAll()` behind a Material 3 `AlertDialog` confirmation, with success/error snackbars. (f) Tests: 5 cases for `gcMissingPacks` + `wipeAll`, 4 cases for `syncToServer` (happy / preserveId / 500 / malformed body), and 2 `AppState` tests asserting `syncToServer` is called with the right `preserveId` for both pack and https content. **Out of scope:** sync triggered by WS messages, periodic background sync, server-side eviction policies.
- **M8.4 — Cached packs display**: Add a "Cached packs" section to `MainActivityPage` rendered below the Settings card, showing what's currently in the local `PackCache`. Implementation: (a) `PackCache.listCachedPacks()` returns a `List<CachedPackInfo>` by walking the pack root; each entry carries `id`, current version (from the `current` pointer file), the parsed `manifest.json` for that version, and the on-disk size of the `content/` directory. Skip pack dirs whose `current` pointer is missing or whose manifest is unparseable (don't crash on malformed cache state). Implementation note: uses **synchronous** filesystem primitives (`listSync`/`existsSync`/`readAsStringSync`/`lengthSync`) wrapped in an async function, because real async `Directory.list` uses wall-clock OS callbacks that `pumpAndSettle` does not drain in widget tests — sync I/O makes the future resolve on the next microtask. (b) New widget `CachedPacksCard` (in `client/lib/fallback/cached_packs_card.dart`) uses a `FutureBuilder<List<CachedPackInfo>>` to render an M3 `Card` with `ListTile` rows: each row shows the pack id (title), `<type> v<version> · <human-readable size>` (subtitle), and a small refresh `IconButton` in the card header to re-list. Empty state shows "No packs cached yet" in a muted text style. The loading-state placeholder is a static `Text` rather than a `LinearProgressIndicator`, because M3's indeterminate indicator schedules animation frames forever and pins `pumpAndSettle` in widget tests. (c) Mount the card in both the compact and expanded layouts of `MainActivityPage`, below the Settings card. (d) Tests: `PackCache.listCachedPacks` unit tests (4 cases: happy path with sorted output, missing `current` pointer skipped, malformed manifest skipped, empty root) and a `CachedPacksCard` widget test asserting that two synthetic packs render with their ids and the empty case renders the empty string. **Out of scope:** delete buttons (the manual "Clear pack cache" wipes everything, server-side delete propagates via M8.3 auto-sync), pull-to-refresh, sorting/filtering UI.
- **M9 — Remote screen on/off**: Operator-driven device power control. Adds a `screenOn` boolean field to `HyacinthConfig` (default `true`). On the server side: extend the Go `Config` struct, accept it on `PUT /config`, broadcast it on `/ws`, add a Material 3 toggle in the operator UI's Current Config card next to brightness. On the client side: extend `HyacinthConfig.fromJson` + `==`/`hashCode` + `copyWith`. In `DisplayPage._applyPolicy`, when `screenOn` differs from the current state, drive a new `ScreenPower` system module to turn the panel on or off.
   **Two-tier capability strategy** — each tier is a real panel-power op; **there is no cosmetic fallback**. If neither tier is available the operator's screen-off command surfaces as an error in the fallback page and the panel stays on. The user is expected to grant either root or Device Admin during onboarding.
   1. **Root** (preferred when available, no UI required). On a device whose `ConfigStore.getRootAvailable == true` (M8.1's cached probe), shell out via `RootHelper`: `su -c "input keyevent 223"` (KEYCODE_SLEEP) for off, `su -c "input keyevent 224"` (KEYCODE_WAKEUP) for on. Real panel power, instant, no system dialogs.
   2. **Device Admin** (required if root is not available). Register a `HyacinthDeviceAdminReceiver extends DeviceAdminReceiver` in the manifest with `BIND_DEVICE_ADMIN` permission and a `device_admin.xml` declaring no extra policies (lockNow needs no flags). The user activates it via `DevicePolicyManager.ACTION_ADD_DEVICE_ADMIN` — this is a one-time system dialog. Onboarding gets a new step (after the existing root-check step) that fires the activation intent and waits for the user to confirm. Once active, the off path calls `DevicePolicyManager.lockNow()` which actually locks the device and lets the system put the panel to sleep on the next idle tick. Wake side: the activity manifest entry gets `android:showWhenLocked="true"` + `android:turnScreenOn="true"`, and the on path calls `KeyguardManager.requestDismissKeyguard(activity, ...)` after flipping `screenOn` back to true. (These are the same activity flags plan.md's "Android Manifest Essentials" section already calls out as needed for M7.5 — M9 takes them now.)
   The native side picks the first available tier at call time: `setScreenOn(bool)` checks `isInteractive()` first (no-op when already in the target state), then tries root if available, else Device Admin if active, else returns an error code `'no_capability'`. The Dart layer surfaces that error to `AppState`, which adds it to the next HealthCheck pass and lights up the relevant Fix button.
   **Concrete deliverables**: (a) `MethodChannel('io.hyacinth/screen_power')` with methods `isInteractive() -> bool`, `isAdminActive() -> bool`, `requestAdmin() -> bool` (fires the system add-admin dialog and returns whether the admin is now active after the dialog), `setScreenOn(bool) -> String` returning the chosen tier (`'root'` / `'admin'`) on success or throwing a `PlatformException(code: 'no_capability')` if neither tier is available. (b) Kotlin: new `HyacinthDeviceAdminReceiver` class + `res/xml/device_admin.xml`, manifest receiver entry, `showWhenLocked` + `turnScreenOn` on the main `<activity>`. (c) `RootHelper.sleepScreen()` / `wakeScreen()` for tier 1, called from the native screen_power channel handler (not directly from Dart, so the tier-selection logic stays in one place). (d) Dart `system/screen_power.dart` exposing `ScreenPower` with `apply(bool screenOn)` that wraps the channel and surfaces a `ScreenPowerUnavailable` exception when no tier is available. (e) `DisplayPage` calls `ScreenPower().apply(widget.config.screenOn)` from `_applyPolicy`; on `ScreenPowerUnavailable`, sets a flag `_screenPowerError` that the page renders as a small banner over the WebView (red `colorScheme.errorContainer` strip across the top reading "Screen-off requested but no capability — grant Device Admin or root in Settings"). The WebView itself is **not** dimmed or overlaid — the panel stays exactly as the OS leaves it. (f) `HealthCheck` gets a new "Screen-off capability" row with status `ok` if root is cached or the admin is active, `fail` otherwise. The Fix button calls `requestAdmin()`. (g) Onboarding gets a new optional step (between the existing root-check step and notifications) titled "Device admin" that explains the screen-off use case and offers a `FilledButton` "Grant" which calls `requestAdmin()` and a `TextButton` "Skip" that warns about the consequence. The step is auto-skipped if the M8.1 root path already granted root. (h) Operator UI: a Material 3 `<md-switch>` toggle in the Current Config card labeled "Display on". (i) `HyacinthConfig` extended with `bool screenOn` defaulting to `true`; `==`/`hashCode`/`copyWith`/`fromJson`/`toJson` updated and tested.
   **Tests**: server `httptest` for `PUT /config` round-tripping `screenOn`, server WS test asserting the broadcast carries it, `HyacinthConfig` unit tests for the new field (parse / default / equality / copyWith), `ScreenPower` MethodChannel wrapper tests (mocked channel: root tier success, admin tier success, no-op-when-matching short-circuit, `no_capability` throws `ScreenPowerUnavailable`, requestAdmin success/decline), `DisplayPage` widget test asserting that `screenOn=false` does NOT rebuild the cached `HyacinthWebView` (assert same `Element` identity across the toggle, same pattern as M3's reload-guard test) and that on `ScreenPowerUnavailable` the error banner is rendered, `HealthCheck` test for the new row (ok when root cached, ok when admin active, fail otherwise), and an onboarding widget test for the new Device Admin step (skipped when root granted, shown otherwise; tap Grant calls the fake's `requestAdmin`).
   **Out of scope:** scheduled screen-off (e.g. cron-style "off after 23:00"), motion-triggered wake, multi-display, deep-sleep / Doze interaction, full M7.5 keyguard-bypass (the keyguard handling here is only what's needed for the wake round-trip — M7.5's broader `setKeyguardDisabledFeatures` is still its own milestone).
- **M9.1 — Screen on/off as imperative commands + wake fix**: M9 modeled `screenOn` as a persistent field on `HyacinthConfig` and let `DisplayPage._applyPolicy` reconcile it on every config push. Two problems: (1) it's the wrong shape — screen on/off is an *imperative one-shot command*, not a piece of state the operator wants to persist (the next config push shouldn't re-flip the screen), and (2) the admin-tier wake path was broken — `requestDismissKeyguard()` only dismisses the lock screen, it does NOT power the panel back on, so on a non-rooted device with Device Admin the off worked but the on did nothing. This milestone reshapes M9: (a) Remove `screenOn` from `Config` (Go) and `HyacinthConfig` (Dart); rip out the related `==`/`hashCode`/`copyWith`/`fromJson`/`toJson` plumbing and the M9 round-trip server tests; remove the `<md-switch>` from the operator UI and the `_applyPolicy`-calls-ScreenPower wiring in `DisplayPage`. (b) Add `POST /screen` to the server that takes `{"action": "on" | "off"}` and broadcasts a new envelope `{"type": "screen_command", "action": "on"|"off"}` to all connected WS clients. The endpoint is auth-gated (M8 token middleware applies). It does NOT touch the persisted `Config`. (c) Operator UI: replace the switch with two Material Web `<md-filled-button>` controls "Screen off" / "Screen on" that POST to `/screen`. Show a small toast on success/failure. Tooltip: "Imperative — fires once and forgets. State isn't persisted." (d) Client `WsClient`: extend the envelope dispatcher to recognize `screen_command` and invoke a new callback `onScreenCommand(bool on)` (added as a constructor parameter alongside the existing `onConfigUpdate`). Forward-compat unknowns still ignored. (e) `AppState` wires `onScreenCommand` to a private `_handleScreenCommand(bool)` that calls `ScreenPower().apply(on)`, catches `ScreenPowerUnavailable`, and stores the most recent error in a new `String? screenPowerError` field exposed via getter. The error clears on the next successful screen command. (f) `DisplayPage` reads `appState.screenPowerError` instead of its own state field and renders the M3 `errorContainer` banner identically to before. (g) **Wake fix at the native layer**: in `MainActivity.kt`'s `setScreenOn` admin tier, before calling `requestDismissKeyguard`, acquire a brief `PowerManager.WakeLock(FULL_WAKE_LOCK | ACQUIRE_CAUSES_WAKEUP | ON_AFTER_RELEASE, "Hyacinth:wake")` for ~3 seconds and release it. These wake-lock flags are deprecated since API 17 but remain the only sandboxed-app way to power the panel back on from a paused activity; the activity flags `showWhenLocked` + `turnScreenOn` only fire when the activity becomes visible, which doesn't happen unless the system already woke the device. Keep the `requestDismissKeyguard` call after the wake lock since the system may still show the keyguard. (h) Document the deeper limitation in the operator UI tooltip and in `README.md`: after `lockNow()`, the Flutter activity is paused but the Dart isolate keeps running thanks to the M8 foreground service, so the WS connection survives long enough for an in-window screen-on command to fire — but once Doze kicks in and the OS kills the TCP socket, only root's `KEYCODE_WAKEUP` reliably wakes the device. (i) Tests: server `httptest` for `POST /screen` happy path + auth gate + WS broadcast assertion; remove the M9 `screenOn`-on-config tests; new client tests for `WsClient` dispatching the `screen_command` envelope to the right callback; new `AppState` test asserting `_handleScreenCommand` updates `screenPowerError` on `ScreenPowerUnavailable`; the `DisplayPage` widget test now drives the error banner via `appState.screenPowerError` instead of `widget.config.screenOn`. **Out of scope:** scheduled / cron-style screen control (still M9.x territory if needed), turning the wake fix into a full M7.5 keyguard-bypass setup.
- **M9.2 — Operator UI redesign**: The inlined operator HTML in `server.go`'s `indexHTML` const works but is utilitarian — flat cards, single column, generic spacing, no real visual hierarchy beyond the cards-on-background pattern. This milestone is a pure presentation-layer redesign: same endpoints, same JS behavior (config form, screen buttons, pack list, WS dirty handling, token, toast), Material You components and tokens unchanged. What changes: (a) typography hierarchy with a distinctive display face for headings + a refined body face (load via Google Fonts), generous letter-spacing on the page title, weight contrasts; (b) layered M3 surface containers — `surfaceContainerLowest` for the page bg, `surfaceContainer` for the section group, `surfaceContainerHigh` for active cards, `surfaceContainerHighest` for input fields — instead of the current single elevation; (c) sticky header with the connection pill, host, and a "Token" `<md-icon-button>`; (d) section grouping reorganized to "Display" (content URL + brightness + timeout) / "Power" (the screen-on/off buttons with their imperative-hint chip) / "Packs" (upload + list) / "Live updates" (status + log); (e) a top status strip showing the current `content` URL truncated and a green/red dot for WS connection — at-a-glance "is the tablet getting what we sent" check; (f) brightness slider gets a live numeric label and a clearer disabled state when "Auto" is on; (g) the pack list becomes a proper M3 `<md-list>` with type-icon avatars, version badge chips, and the "Use as content" / "Delete" actions inside `<md-icon-button>` trailing slots so each row reads like a real list item not a row of buttons; (h) staggered fade-in motion on initial load via CSS `animation-delay` on each card (Material 3 emphasized-decelerate easing curve), no JS animation library; (i) a refined dark scheme via `prefers-color-scheme: dark` using the `--md-sys-color-*` tokens; (j) responsive layout: single column on phones, two-column (Display+Power left, Packs right) at 720px, three-column (Display | Power+Live | Packs) at 1100px. **Out of scope:** any server endpoint changes, any client-side Flutter changes, any new dependencies beyond what `@material/web` already pulls in. The HTML stays a single `const indexHTML` in `server.go`. Update the existing M4 `TestGetIndexReturnsHTML` to assert against the new string markers (the existing test pins `<md-filled-button>` and the import map — those stay; if the test pins anything else specific, update it). No new tests beyond updating the existing index test.
- **M9.3 — Operator UI fixes (empty-state + auto type-detect)**: Two operator-side fixes flagged immediately after M9.2 shipped. (a) The Packs section was rendering the "No packs uploaded yet" empty-state alongside real list items. Cause: the `.pack-empty` class set `display: flex` which won the cascade against the user-agent `[hidden] { display: none }` (same specificity, our rule comes later). Add an explicit `.pack-empty[hidden], md-list[hidden] { display: none !important; }` rule. (b) The upload form had a `<md-outlined-select>` for pack type ("png" / "jpg" / "webp" / "gif" / "zip") that the operator had to set manually before clicking Upload. The type can be derived from the file's MIME and extension. Remove the select entirely. Add a `packTypeFromFile(file)` helper in the JS that maps `application/zip`/`.zip` → `zip`, image MIMEs → the corresponding image type, returns null otherwise. The upload handler calls it after the file is chosen and toasts an error for unsupported types. Server contract is unchanged — the `type` form field is still required, the JS just fills it automatically. **Out of scope:** redesigning the native file input (still uses the browser's localized "选择文件" / "Choose file" UI), drag-and-drop upload, multi-file upload.
- **M9.4 — Operator UI mobile overflow fix**: After M9.3 the Packs card was still overflowing the viewport horizontally on phone-width screens. Two root causes: (1) Material Web's `<md-outlined-text-field>` and `<md-outlined-select>` are `inline-flex` by default and don't stretch to fill a grid/flex column, so the Pack ID field kept its intrinsic min-width and pushed the parent card past the viewport; (2) the native `<input type="file">` on Chinese-locale Chrome shows "选择文件 未选择任何文件" which has a larger intrinsic minimum width than the English equivalent, and even with `min-width: 0; flex: 1` the user-agent enforces a floor that exceeds a 360dp phone viewport. Fixes: (a) global `md-outlined-text-field, md-outlined-select { width: 100%; }` so Material Web text fields stretch to their containers — applies project-wide, not just packs; (b) replace the native file input with the standard "visually hidden but accessible" pattern (`position: absolute; clip: rect(0,0,0,0); width: 1px`) inside the wrapping `<label>`, plus a custom `<span class="file-label">` that shows the chosen filename and a default "Choose a file (zip or image)" placeholder. The label is updated by a `change` listener on the input. Reset on successful upload. (c) Defensive `html, body { overflow-x: clip; }` so any future runaway child can't make the whole page horizontally scroll. **Out of scope:** drag-and-drop upload, multi-file upload, file-size preview before upload.
- **M9.5 — Operator UI: zero-step upload + auto-publish on pack select**: Two more UX redundancies removed. (a) The Packs upload form had a separate "Upload" `<md-filled-button>` that the operator clicked after picking a file — but the file picker is itself a deliberate user action, so the second click is pure friction. Drop the Upload button entirely; the file input's `change` event triggers `uploadPack(file)` directly. The Pack ID must be filled first; if not, toast "Enter a pack id first" and clear the file selection so the user can re-pick after entering an id. The label briefly shows "Uploading <filename>…" while the request is in flight, then resets on success or error. A `let uploading = false` re-entry guard prevents double-fires from rapid file changes. (b) Clicking the play_arrow ("Use as content") icon on a pack row used to write the URL into the content field and toast "click Save to push", forcing a second action. Now `setContentToPackUrl(p)` extracts a reusable `saveConfig()` function (the Save button's existing handler is just `addEventListener('click', saveConfig)`) and calls it directly after setting the URL, so a single click promotes the pack to live. The toast becomes "Now showing <id>". **Out of scope:** drag-and-drop upload, undo for the auto-publish action, multi-pack upload.
- **M9.6 — Operator section reorder (Display / Packs / Power / Live)**: Packs is the second-most-frequently-touched section after Display — operators upload and switch packs constantly, while Power is occasional and Live updates is purely a debugging surface. The M9.2 redesign put Packs third on the right column, which buried it. Reorder to: Display / Packs / Power / Live, both in source order and in the responsive grid. Source: `<main class="grid">` children rearranged. Medium breakpoint (720dp) `grid-template-areas: "display packs" / "power live"`. Wide breakpoint (1100dp) flipped from `1fr 1fr 1.4fr` to `1fr 1.4fr 1fr` so Packs gets the wider middle column (the pack list is the tallest piece after Display), with Display spanning both rows on the left and Power+Live stacked on the right. Animation-delay sequence updated to match the new order. **Out of scope:** any layout change beyond the reorder, any per-section visual tweaks.
- **M9.7 — Fold Power into Display, drop the section**: The Power section was a whole card hosting two buttons + an explanatory paragraph. The screen on/off controls conceptually belong with the rest of the "do something to the display" actions, so move them into Display's existing actions row to the left of Save and delete the Power card entirely. Concrete changes: (a) The two `<md-outlined-button>`s for screen on/off (both outlined now, no `<md-filled-button>` for screen-on — Save is the only filled/primary action in the row) live inside `<div class="actions">` with a `<span class="spacer"></span>` between them and Save so the visual hierarchy reads left→right as "do once" → "do once" → "save". A `title` attribute on each button carries the "Imperative — fires once, not persisted" hint that the deleted explanatory paragraph used to provide. (b) `<section class="card sect-power">`, `.power-buttons`, `.chip`, the `h-power` heading, the `sect-power` grid-area, and the corresponding animation-delay rule are all deleted. (c) The 720dp grid collapses from 4 sections in 2×2 to 3 sections with `"display packs" / "live packs"` (Packs spans both rows on the right). The 1100dp grid simplifies to a single row `"display packs live"` with `1fr 1.4fr 1fr`. (d) `.actions` gains `align-items: center` and `flex-wrap: wrap` so the row degrades gracefully on narrower phones — when the buttons can't fit on one line, the spacer becomes a wrap point and Save drops to its own row. **Out of scope:** any redesign of the Display section's input controls themselves; the brightness row, content URL, and timeout select are unchanged.

After M3 you have a usable always-on display driven by the server. M5–M6 add offline-capable rich content without further client work.

## Critical Files
- `server/server.go`
- `client/android/app/src/main/AndroidManifest.xml`
- `client/android/app/src/main/res/xml/network_security_config.xml`
- `client/lib/main.dart`
- `client/lib/app_state.dart`
- `client/lib/display/webview_controller.dart` (the reload-guard lives here)
- `client/lib/resource_pack/pack_manager.dart`
- `client/lib/resource_pack/scheme_handler.dart`

## Verification (per milestone)
- **M0**: `go run server.go` → `curl localhost:PORT/config` returns JSON; Flutter app prints same JSON.
- **M1**: Install on tablet, launch shows fullscreen immersive WebView of a known site; screen does not sleep.
- **M2**: Press Home → Hyacinth is offered as launcher; revoking a permission flips UI to Fallback; "Test connection" works.
- **M3**: With WebView showing content, `PUT /config` changing only brightness updates brightness with **zero WebView reload** (verify by playing a video and confirming no interruption); changing `content` reloads.
- **M5/M6**: Upload an image pack via operator UI on phone; set config `content` to `hyacinth://pack/<id>/...`; tablet (on Wi-Fi) downloads, caches, displays. Toggle to mobile data → no re-download. Republish new version → tablet swaps on next `config_update`.
- **M8**: Leave tablet on for 24h with screen off cycles; WS reconnects survive Doze; immersive returns after dialogs.
